# Copy to .env and adjust for local or Docker deployment.

# Ollama HTTP API (default matches local Ollama)
OLLAMA_HOST=http://127.0.0.1:11434

# Model tag as shown by `ollama list`
OLLAMA_MODEL=deepseek-r1:1.5b

# Flask / gunicorn (Dockerfile uses 8080)
PORT=8080
